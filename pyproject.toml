[project]
name = "codespace"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "akaig", email = "73285780+Akalroop@users.noreply.github.com" }
]
requires-python = ">=3.13"
dependencies = [
    "openai>=3.3.1",
    "pymupdf4llm>=1.28.2",
    "pypdf>=6.16.2",
]

[project.scripts]
codespace = "codespace:main"

[build-system]
requires = ["uv_build>=0.12.0,<0.13.0"]
build-backend = "uv_build"
